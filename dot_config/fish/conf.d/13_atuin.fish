# conf.d/13_atuin.fish — history sync cross-machine
# Prima volta: `atuin register` (cloud) oppure `atuin login` se già hai un account
# Self-hosted: configura ~/.config/atuin/config.toml
atuin init fish | source
